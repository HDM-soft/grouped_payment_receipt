<odoo>
    <record id="action_server_generate_grouped_payment_receipt" model="ir.actions.server">
        <field name="name">Generate Grouped Payment Receipt</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
            action = records.action_generate_grouped_payment_receipt()
        </field>
    </record>
</odoo>